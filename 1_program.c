#include<stdio.h>
#include<math.h>
//
int main()
{float r,t;
    int x;
    printf("input the x value");
    scanf("%i ",&x);
    int y;
    printf("input the y value");
    scanf("%i ",&y);
    r = sqrt(x*x+y*y);
    printf(" show the square = %i ",r);
    t = cos(30                                                                        );
    printf("%i ",t);
    return 0;
}
